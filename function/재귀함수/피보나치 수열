#include <stdio.h>

int fib(int n){
if(n>=3) {
	return fib(n-2)+fib(n-1);
}
else return 1;
	}

int main() {
  int num;
  printf("Enter a number N.\n");
  scanf("%d", &num);
  printf("%dth fibonacci number is %d.\n", num, fib(num));
  return 0;
}
